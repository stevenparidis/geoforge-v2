N05 — Valid: intrusion younger than host (NEGATIVE)

Tests that a geologically valid batholith intrusion (implicitly younger, intruding into pre-existing layers)
does NOT show .validation-note-pill.

This is the correct counterpart to M06, confirming that a valid cross-cutting relationship does not
trigger a warning.
